App Router patch (use if your repo has an /app folder)

Install:
1) Unzip
2) Copy into repo root (merge/overwrite):
   - styles/
   - components/
   - app/
   - public/images/students/
   - tailwind.config.js
