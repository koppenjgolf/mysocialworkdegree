App Router missing routes patch

This patch adds the routes referenced by your navbar and homepage:
- /degrees
- /careers
- /states

How to install:
1) Unzip.
2) Upload the `app/` folder into the ROOT of your repo (merge/overwrite).
3) Commit + push. Vercel will redeploy.

Note: This patch does not remove anything; it only adds missing route files.
